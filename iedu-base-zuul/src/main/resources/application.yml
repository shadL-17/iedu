eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/

server:
  port: 8080

spring:
  application:
    name: iedu-base-zuul

zuul:
  routes:
    api-sso:
      path: /user/**
      serviceId: iedu-user-sso
  sensitive-headers:
  host:
    connect-timeout-millis: 100000
    socket-timeout-millis: 100000

